<!DOCTYPE html>
<html>
  <head>
    <title>ProjectTask</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <main class="">
      <div data-controller="close"  data-close-timeout-value= "3000" data-close-hidden-class= "hidden" data-close-target="alert" >
        <% if notice.present? %>
          <p class="bg-green-600 h-8 text-gray-200 font-semibold  rounded-md px-4 absolute bottom-6 right-6 flex items-center justify-center "><%= notice %>
            <span  data-action="click->close#hide" class ="cursor-pointer">
              <%= inline_svg_tag("close.svg") %>
            </span>
          </p>
        <%elsif alert.present? %>
          <p  class="bg-red-600 h-8 text-gray-200 font-semibold  rounded-md px-4 absolute bottom-6 right-6 flex items-center justify-center"><%= alert %>
            <span data-action="click->close#hide" class ="cursor-pointer">
              <%= inline_svg_tag("close.svg") %>
            </span>
          </p>
        <% end %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
